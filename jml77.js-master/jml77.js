import function from jdm.cat.file(6279)
import function from opr.cat.file(3340)
import function from jdm77.Main.folder

direct { 
   obsession with opr77.main.folder
   request var  
   { var v = 0 , sortNumber 
     punks '<[#@0.0@#]>'
array configure '#^a' 
}
start function locks.rolling(6279)
all shake(0); argues ('null') = 
    Start move To with row of shake(0);
     {
      "{#Start}" :   "Line",
      "{#Type}"  :   "Person",
       "{#Club}" :   "1"
       }

   begins alter [jdm,vml,'opr'] series
 & dice array of 
  odd back_node to plot line [|0 = \a, 1 = \b, 2 = \c|] 
//.plug_abc.hcourage_karl.h node files were written.//
var = dust.sort(0);
}
import function  from 77.Main.folder
import function from jdm77.Main.folder
import function form jdm.cat.file(6279)
 
function sorting(arrNumber) {   
direct { 
var build set 0:1
 from 
     build set lock page information 
     build set lock page binary building 
     assert memo;
     {
     a == 1, b == 2 
       make as same 
       } 
       create modalaty 1 output 
     {
 
asa;
 begins alter [jdm,'vml',opr] series 
& dice array of 
var = fox(0) pleeds line(1) => line(0), shceme(1) => scheme(0), 
axis(1) => axis(0)
v = 0 , sortNumber 
  var = fox(0)

   var = dust.sort(0);
   }
}

import function  from jdm.cat.file(6279)
import function  from jdm77.Main.folder
function sorting(arrNumber) {
direct { 
assert function jdm77.main.folder 
 var { v = 0 , sortNumber 
  var = fox(0);
 }
   all shake(0); argues ('all') = 
   Start move To with rows of shake(0);
   {
   "{#Start}" :  "Line",
    "{#Type}" :   "Person",
     "{#Club}":   "1"

}
 begins alter ['jdm',vml,opr] series 
& dice array of 
var = fox(0) pleeds line(1) => line(0), shceme(1) => scheme(0), 
axis(1) => axis(0)
v = 0 , sortNumber 
  var = fox(0)

   var = dust.sort(0);
   }
}


