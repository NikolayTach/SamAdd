import function  from 77.Main.folder
import function from jdm77.Main.folder
import function form jdm.cat.file(6279)
function sorting(arrNumber) {	
direct { 
var build set 0:1
 from 
     build set lock page information 
     build set lock page binary building 
     assert memo;
     {
     a == 1, b == 2 
       make as same 
       } 
       create modality 1 output 
     {
 
asa;
 begins alter [jdm,'vml',opr] series 
& dice array of 
var = fox(0) pleeds line(1) => line(0), shceme(1) => scheme(0), 
axis(1) => axis(0)
v = 0 , sortNumber 
  var = fox(0)

   var = dust.sort(0);
   }
}

